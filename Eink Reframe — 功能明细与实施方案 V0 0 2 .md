# Eink Reframe — 功能明细与实施方案 V0.0.2

# 项目概述

基于 **Seeed Studio XIAO ESP32S3 Sense**（含 OV2640 摄像头）与 **2.9 英寸 SSD1680 三色电子墨水屏**（黑/白/红），打造一款便携式即拍即显电子墨水相机。核心理念为「即拍即显，看完就忘」——按下按钮拍照后立即显示在墨水屏上，拍完关机断电零功耗。不使用 SD 卡，全部使用 ESP32S3 板载 Flash 存储。灵感来源于 [reframe-camera](https://github.com/kaloyaan/reframe-camera) 项目（Raspberry Pi + Waveshare 7 色屏），针对 ESP32 平台进行重新设计与适配。

---

# 一、功能明细

## 1.1 核心功能

| **功能模块** | **详细描述** |
| --- | --- |
| 即拍即显 | 按下按钮后，摄像头采集图像，经抖动处理后立即显示在电子墨水屏上，端到端延迟目标 < 3 秒 |
| 图像抖动处理 | 将 OV2640 采集的彩色图像转换为黑/白/红三色，采用 Floyd-Steinberg 误差扩散抖动算法，在 128x296 分辨率上呈现最佳视觉效果 |
| 照片存储 | JPEG 保存至 ESP32S3 板载 Flash（LittleFS），当前显示照片始终保留，超出上限自动删除最旧照片 |
| 单键操作 | 专用拍照按键完成拍照，短按即拍即显，通过物理电源开关控制开关机 |
| LED 状态指示 | 绿色 LED 指示设备状态：常亮=就绪待拍，快闪=采集处理中，熄灭=墨水屏刷新中，短亮一次=拍照完成 |
| 电池供电 | 3.7V 锂电池，USB-C 充电，物理电源开关控制通断电 |

## 1.2 模块功能分解

### 1.2.1 摄像头采集模块

- **传感器**：OV2640（XIAO ESP32S3 Sense 内置），支持 UXGA(1600x1200) JPEG 输出
- **采集模式**：按需采集（按钮触发），非连续视频流
- **分辨率配置**：采集 QVGA(320x240) 或 VGA(640x480)，经裁剪/缩放适配 128x296 屏幕比例
- **图像格式**：JPEG → 解码为 RGB565 → 转换为 RGB888 进行处理
- **依赖库**：`esp32-camera`（Espressif 官方维护）

### 1.2.2 图像处理模块

- **色彩量化**：将 RGB888 像素量化为黑/白/红三色
    - 红色判定：R 分量显著高于 G/B 分量且 R > 阈值
    - 亮度判定：灰度值 < 128 → 黑，灰度值 >= 128 → 白
- **抖动算法**：Floyd-Steinberg 误差扩散
    - 误差分配：右(7/16)、左下(3/16)、下(5/16)、右下(1/16)
    - 参考项目 `aitjcize/esp32-photoframe` 的实测色板思路，对 SSD1680 实际显示颜色进行测量校准
- **图像缩放**：双线性插值将摄像头原始分辨率缩放至 128x296
- **内存优化**：逐行处理，避免全帧 RGB888 缓存（ESP32S3 PSRAM 8MB 可支持全帧处理）

### 1.2.3 显示控制模块

- **驱动芯片**：SSD1680Z
- **屏幕参数**：128x296 像素，黑/白/红三色
- **刷新方式**：全屏刷新（SSD1680 三色屏不支持局部刷新）
- **刷新时间**：三色（黑/白/红）全屏刷新 **15~27 秒**（Waveshare 官方标称 15s，GxEPD2 驱动实测约 27s）；黑白双色全屏刷新仅 **1.75~3.2 秒**（可作为快速预览模式）
- **显示缓冲**：双缓冲架构，总缓冲 **9472 bytes**
    - 黑白缓冲区（_black_buffer）：128x296/8 = 4736 bytes
    - 红色缓冲区（_color_buffer）：128x296/8 = 4736 bytes
- **驱动库**：`GxEPD2` 库中的 `GxEPD2_290_C90c` 类（SSD1680 三色屏驱动，GDEM029C90 面板），或 Waveshare 官方 SSD1680 驱动
- **SPI 配置**：4-wire SPI，CLK 频率建议 4-10MHz

### 1.2.4 存储管理模块

- **存储介质**：ESP32S3 板载 8MB Flash（LittleFS 文件系统）
- **不使用 SD 卡**，原因：
    - 场景定位为「即拍即显」，无需大量本地存储
    - 去掉 SD 卡释放 GPIO 7/8/9（原 SD 卡 SPI 总线），解决 EPD 引脚不足问题
    - 简化硬件设计，减少接线复杂度
- **存储策略**：
    - 当前显示照片：始终保留 1 张
    - 历史照片：保留最近 20~50 张 JPEG（可配置）
    - 超出上限自动删除最旧照片
- **文件结构**（LittleFS）：
    - `/photos/current.jpg` — 当前显示的照片
    - `/photos/` — 最近照片队列
    - `/config/settings.json` — 系统配置文件
- **文件命名**：序号命名 `00001.jpg` ~ `NNNNN.jpg`（不依赖 RTC 时间戳，见附录 3.3）
- **存储空间估算**：
    - Flash 可用约 1.5~2MB（扣除固件 + OTA 分区）
    - OV2640 QVGA JPEG 约 5~15KB → 可存约 **100~300 张**
    - OV2640 VGA JPEG 约 20~40KB → 可存约 **40~80 张**
    - 处理后位图 BMP（128x296）约 9KB → 仅当前 1 张保留
- **PSRAM 说明**：8MB PSRAM 仅用于运行时图像处理（解码/缩放/抖动），深度睡眠后数据丢失，不作持久存储

### 1.2.5 按键与指示灯模块

- **拍照按键**：专用轻触开关 6x6mm + 按键帽，接 GPIO 6 (D5)，外接至外壳侧面，模拟相机快门手感
- **绿色状态灯**：3mm 绿色 LED + 220Ω 限流电阻，接 GPIO 5 (D4)
- **操作逻辑**：
    - 电源开关：打开→上电→待机（绿灯常亮）；关闭→断电→零功耗
    - 拍照按键：短按拍照→绿灯快闪（采集处理中）→屏幕刷新（绿灯灭）→绿灯短亮一次（完成）→回常亮待机
- **LED 状态定义**：
    - 🟢 常亮：设备就绪，等待拍照
    - 🟢 快闪：图像采集 + 抖动处理中
    - ⚫ 熄灭：墨水屏刷新中（15~27 秒）
    - 🟢 短亮一次：拍照完成，回到就绪
- **防抖处理**：软件消抖 20ms

---

# 二、项目实施方案

## 2.1 硬件清单

| **组件** | **型号/规格** | **备注** |
| --- | --- | --- |
| 主控板 | Seeed Studio XIAO ESP32S3 Sense | 含 OV2640 摄像头扩展板，8MB PSRAM，8MB Flash |
| 电子墨水屏 | 2.9" SSD1680 三色屏 (128x296) | 黑/白/红三色，4-wire SPI，8Pin 排针接口 |
| 电源开关 | 滑动开关/按钮开关 | 电池与主板之间串联，物理断电零功耗 |
| 电池 | 3.7V 锂电池 600-1000mAh | XIAO ESP32S3 内置充电管理 |
| 拍照按键 | 轻触开关 6x6mm + 按键帽 | 专用快门按键，GPIO 6 (D5)，带按键帽提升按压手感 |
| 状态指示灯 | 绿色 LED 3mm + 220Ω 电阻 | GPIO 5 (D4)，状态指示：常亮=就绪，快闪=处理中，灭=刷新中 |
| 外壳 | 3D 打印外壳 | 预留屏幕窗口、摄像头孔、按钮孔 |

## 2.2 软件技术栈

| **层级** | **技术选型** | **说明** |
| --- | --- | --- |
| 开发框架 | Arduino IDE / PlatformIO | 基于 ESP32 Arduino Core，开发效率高 |
| 摄像头驱动 | esp32-camera 库 | Espressif 官方维护，原生支持 OV2640 |
| 屏幕驱动 | GxEPD2 库 | ZinggJM 维护，1.4k+ Stars，支持 SSD1680 |
| 图像处理 | 自实现 C/C++ 代码 | Floyd-Steinberg 抖动 + 三色量化，参考 aitjcize/esp32-photoframe |
| 文件系统 | LittleFS（板载 Flash） | 照片 + 配置均存 Flash，约 1.5~2MB 可用空间 |

## 2.3 参考项目与依赖

| **项目** | **Stars** | **参考价值** |
| --- | --- | --- |
| [aitjcize/esp32-photoframe](https://github.com/aitjcize/esp32-photoframe) | 179 | ESP32 e-ink 固件，实测色板 + Floyd-Steinberg 抖动算法（C 实现），RESTful API，Web 界面，深度睡眠。**核心参考** |
| [waveshareteam/ESP32-S3-PhotoPainter](https://github.com/waveshareteam/ESP32-S3-PhotoPainter) | 51 | Waveshare 官方 ESP32-S3 e-ink 固件，aitjcize 的上游基础，硬件参考 |
| [ZinggJM/GxEPD2](https://github.com/ZinggJM/GxEPD2) | 1460 | Arduino e-ink 显示库，GxEPD2_290 类直接支持 2.9" SSD1680 三色屏 |
| [0015/7-Color-E-Paper-Digital-Photo-Frame](https://github.com/0015/7-Color-E-Paper-Digital-Photo-Frame) | 114 | ESP32 + e-ink 相框，Flask 服务端图像处理思路参考 |
| [aitjcize/epaper-image-convert](https://github.com/aitjcize/epaper-image-convert) | 8 | Node.js CLI 图像转换工具，高级抖动算法参考 |
| [espressif/esp32-camera](https://github.com/espressif/esp32-camera) | - | Espressif 官方摄像头驱动库，OV2640 原生支持 |
| reframe-camera (本地项目) | - | 原始 Raspberry Pi 版本，架构设计与产品理念参考 |

## 2.4 软件架构设计

### 2.4.1 系统状态机

**流程**：打开物理开关 → 上电初始化 → 待机模式（🟢常亮）→ 按下拍照按键 → 摄像头采集（🟢快闪）→ 图像解码 → 三色抖动 → 刷新墨水屏（⚫灭）→ 保存到 Flash → 🟢短亮一次 → 回到待机（🟢常亮）→ 关闭开关断电

### 2.4.2 核心模块数据流

**流程**：OV2640 摄像头 → JPEG 帧 → JPEG 解码器 → RGB565 → 图像缩放（双线性插值）→ 128x296 RGB888 → Floyd-Steinberg 三色抖动 → 黑白缓冲 + 红色缓冲 → SSD1680 显示驱动；同时保存到 Flash（JPEG + BMP）

### 2.4.3 引脚分配规划

| **功能** | **Xiao Pin** | **GPIO** | **接口** | **备注** |
| --- | --- | --- | --- | --- |
| 摄像头 D0-D7 | — | GPIO 15/17/16/18/12/14/11/48 | DVP 8-bit | Sense 板载摄像头数据引脚 |
| 摄像头 XCLK | — | GPIO 10 | DVP | 摄像头时钟输出（XCLK） |
| 摄像头 PCLK | — | GPIO 13 | DVP | 摄像头像素时钟 |
| EPD SCL (SCK) | D8 | GPIO 7 | SPI | 硬件 SPI SCK |
| EPD SDA (MOSI) | D10 | GPIO 9 | SPI | 硬件 SPI MOSI |
| EPD CS | D0 | GPIO 1 | SPI | 片选 |
| EPD DC | D1 | GPIO 2 | GPIO | 数据/命令 |
| EPD RES | D2 | GPIO 3 | GPIO | 复位 |
| EPD BUSY | D3 | GPIO 4 | GPIO | 忙检测 |
| 绿色状态 LED | D4 | GPIO 5 | GPIO | 状态指示灯（常亮=就绪，快闪=处理中，灭=刷新中） |
| 拍照按键 | D5 | GPIO 6 | GPIO | 专用拍照快门按键（带按键帽，外接至外壳侧面） |

## 2.5 开发阶段与里程碑

### 阶段一：硬件验证与环境搭建（1 周）

- [ ]  搭建 XIAO ESP32S3 Sense + OV2640 摄像头测试环境
- [ ]  验证 esp32-camera 库采集 JPEG 图像，串口输出分辨率与帧率
- [ ]  连接 2.9" SSD1680 屏幕到 ESP32S3 SPI 引脚
- [ ]  使用 GxEPD2 库显示测试图案（纯色、渐变、文字）
- [ ]  验证 Flash 空间（LittleFS）可用容量
- [ ]  确认引脚分配无冲突，记录可用引脚清单

### 阶段二：图像处理核心（1-2 周）

- [ ]  实现 JPEG → RGB565 解码（利用 esp32-camera 内置 JPEG 解码）
- [ ]  实现双线性插值图像缩放至 128x296
- [ ]  实现黑/白/红三色量化算法
- [ ]  实现 Floyd-Steinberg 误差扩散抖动
- [ ]  生成黑白/红色双缓冲区位图
- [ ]  对比不同抖动参数效果，选择最佳默认值
- [ ]  参考 aitjcize/esp32-photoframe 的实测色板思路，校准 SSD1680 实际显示颜色

### 阶段三：拍照与显示集成（1 周）

- [ ]  实现按钮触发拍照流程（采集 → 处理 → 显示）
- [ ]  优化端到端延迟（目标 < 3 秒采集+处理，屏幕三色刷新约 15~27 秒为硬件限制）
- [ ]  实现照片保存到 Flash（JPEG 原图 + 当前显示 BMP）
- [ ]  添加拍照状态指示（屏幕刷新中显示“处理中”提示）

### 阶段四：电源开关与集成（1 周）

- [ ]  设计电池与主板之间的物理开关电路（滑动开关或按钮开关）
- [ ]  验证开关通断对 ESP32S3 启动/关机的影响
- [ ]  测量工作电流，确认电池续航估算
- [ ]  确保上电启动速度尽可能快（从开关打开到拍照就绪）

### 阶段五：外壳与集成（1 周）

- [ ]  设计 3D 打印外壳（屏幕窗口、摄像头孔、拍照按键孔、LED 指示灯孔、USB-C 开口）
- [ ]  组装硬件到外壳
- [ ]  整体功能测试与调优
- [ ]  编写使用说明文档

## 2.6 风险与应对措施

| **风险** | **影响等级** | **应对措施** |
| --- | --- | --- |
| 引脚冲突（摄像头占用大量 GPIO） | 高 | 不使用 SD 卡释放 GPIO 7/9 给 EPD SPI；摄像头占用 14 个 GPIO，剩余空闲引脚 5 个；本项目不使用电池电量监测，改用物理电源开关控制通断电 |
| PSRAM 不足导致全帧处理 OOM | 中 | 逐行处理模式；利用 ESP32S3 的 8MB PSRAM；采集 QVGA 而非 VGA 减少内存占用 |
| 三色屏刷新慢（15~27s）影响体验 | 中 | 刷新期间显示"处理中"动画/提示；未来可考虑黑白快速刷新模式（SSD1680 支持黑白 1-2s 刷新） |
| 三色抖动效果不佳（红色偏色） | 中 | 参考 aitjcize 实测色板方案，对 SSD1680 红色进行实际测量校准；调整量化阈值 |
| Flash 空间不足（约 1.5~2MB） | 低 | 优化 JPEG 压缩质量；自动删除最旧照片 |
| 电池续航不足 | 低 | 物理电源开关断电零待机；选用 1000mAh 以上电池；优化单次拍照耗电量 |

## 2.7 与 reframe-camera 原项目的关键差异

| **维度** | **reframe-camera（原项目）** | **本项目（ESP32S3 适配版）** |
| --- | --- | --- |
| 主控芯片 | Raspberry Pi Zero 2 W | XIAO ESP32S3 (ESP32-S3R8) |
| 摄像头 | CSI 摄像头模块 | OV2640（DVP 接口） |
| 屏幕 | Waveshare 4" 7 色 Spectra 6 | 2.9" SSD1680 三色（黑/白/红） |
| 抖动算法 | 6 色量化 + Floyd-Steinberg + Bayer 混合 | 3 色量化 + Floyd-Steinberg |
| 开发语言 | Python（PIL/NumPy） | C/C++（Arduino 框架） |
| 电源管理 | PiSugar 3 + I2C 按钮读取 + shutdown -h now | 物理电源开关完全断电（零待机功耗） |
| Web 仪表盘 | FastAPI + uvicorn | 本项目不包含 |
| 系统服务 | systemd Type=notify | FreeRTOS 任务调度 |
| 色彩查找表 | 32K CIELAB 最近色 LUT | 3 色直接量化（无需 LUT） |
| 体积 | 较大（Pi Zero + 扩展板） | 紧凑（XIAO 仅拇指大小） |

---

# 三、附录

## 3.1 关键参考文档

- [XIAO ESP32S3 Sense 官方 Wiki](https://wiki.seeedstudio.com/xiao_esp32s3_getting_started/)
- [esp32-camera 库文档](https://github.com/espressif/esp32-camera)
- [GxEPD2 库 Wiki](https://github.com/ZinggJM/GxEPD2/wiki)
- [SSD1680 数据手册](https://www.waveshare.com/wiki/2.9inch_e-Paper_Module)
- [aitjcize/esp32-photoframe 技术文档](https://aitjcize.github.io/esp32-photoframe/)
- [reframe-camera 项目](https://github.com/kaloyaan/reframe-camera)

## 3.2 开发环境配置

```
# PlatformIO platformio.ini 配置参考
[env:xiao_esp32s3]
platform = espressif32
board = seeed_xiao_esp32s3
framework = arduino
board_build.partitions = default.csv
upload_speed = 921600
monitor_speed = 115200
build_flags =
    -DBOARD_HAS_PSRAM
    -DARDUINO_USB_CDC_ON_BOOT=1
lib_deps =
    espressif/esp32-camera
    ZinggJM/GxEPD2
    bblanchon/ArduinoJson
```

## 3.3 时间管理方案

XIAO ESP32S3 **没有板载 RTC 芯片**，深度睡眠后系统时间会重置为 0。针对本项目的时间戳需求，采用以下方案：

- **照片命名**：使用递增序号（`00001.jpg`）而非时间戳，无需 RTC
- **WiFi 在线时**：通过 NTP 服务器获取精确时间，用于照片元数据记录（JSON 文件记录序号 + 时间映射）
- **深度睡眠唤醒**：ESP32 内部 RTC 计时器可在睡眠期间维持时间累加（精度约 ±10%），适合短期计时
- **未来升级**：如需精确离线时间，可外接 PCF8563 RTC 模块（I2C 接口，但需注意 GPIO 40/39 已被摄像头 I2C 占用，需使用软件 I2C 或其他方案）

## 3.4 图像方向适配

屏幕分辨率为 **128x296**（竖屏比例），但摄像头拍摄通常为横屏。适配策略：

- **自动检测方向**：分析图像长宽比，横屏照片自动旋转 90°
- **裁切适配**：将横屏照片裁切为 128:296 比例，居中裁切保留主体
- **缩放策略**：先按比例缩放至短边匹配，再居中裁切至 128x296
- **可选模式**：用户可配置为「全图缩放（加黑边）」或「居中裁切（无黑边）」

## 3.3 测试用例

### 3.3.1 硬件连接测试

| **#** | **测试项** | **操作步骤** | **预期结果** |
| --- | --- | --- | --- |
| HW-01 | 墨水屏 SPI 通信 | 上电后运行 `showReadyScreen()`，观察墨水屏 | 墨水屏显示启动画面：标题"Eink Reframe"、照片数、存储信息、红色"Press button to capture"提示 |
| HW-02 | 摄像头初始化 | 上电后观察串口输出 | 串口输出 `[CAM] OV2640 ready!`，无 `INIT FAILED` 错误 |
| HW-03 | 按钮响应 | 短按 BOOT 按钮 | 串口输出 `[BTN] Pressed`，触发拍照流程 |
| HW-04 | 按钮消抖 | 快速连续按下按钮（间隔 < 500ms） | 每次仅触发一次拍照，无重复触发 |
| HW-05 | LittleFS 挂载 | 上电后观察串口输出 | 输出 `[FS] Total: xxxxx | Used: xxxxx`，容量值合理 |
| HW-06 | 物理电源开关 | 关闭开关后再打开 | 设备完全断电 → 重新上电 → 显示启动画面，照片计数不丢失 |
| HW-07 | 绿色 LED 常亮 | 上电启动完成后观察绿色 LED | LED 常亮绿色，表示设备就绪待拍 |
| HW-08 | 绿色 LED 快闪 | 按下拍照按键后观察 LED | LED 快速闪烁绿色（采集+处理中），持续至屏幕开始刷新 |
| HW-09 | 绿色 LED 状态流转 | 完成一次完整拍照流程，观察 LED 变化 | 常亮→快闪→熄灭→短亮一次→回常亮，状态流转正确 |

### 3.3.2 拍照与显示测试

| **#** | **测试项** | **操作步骤** | **预期结果** |
| --- | --- | --- | --- |
| CAP-01 | 正常拍照 | 对着明亮场景按下按钮 | 屏幕显示"Capturing..." → 15~27 秒后显示三色抖动处理后的照片 |
| CAP-02 | JPEG 采集 | 拍照后观察串口输出 | 输出 `[IMG] JPEG 320x240 (xxxxx bytes)`，JPEG 大小约 5~30KB |
| CAP-03 | 双线性插值缩放 | 拍摄含文字/线条的场景，检查墨水屏输出 | 图像边缘平滑，无明显锯齿和色块（对比最近邻算法） |
| CAP-04 | 三色抖动 — 红色区域 | 拍摄红色物体（红色纸张/红色花朵） | 红色区域呈现红色抖动效果，非全黑或全白 |
| CAP-05 | 三色抖动 — 暗色场景 | 在暗光环境或拍摄黑色物体 | 暗色区域呈现黑色，非全白 |
| CAP-06 | 三色抖动 — 亮色场景 | 拍摄白色墙壁或天空 | 亮色区域呈现白色，非全黑或全红 |
| CAP-07 | 三色抖动 — 灰度渐变 | 拍摄灰度渐变场景（灰色墙面/纸张阴影） | 灰度区域呈现平滑的黑白抖动过渡，无明显色带 |
| CAP-08 | 图像旋转方向 | 正常握持设备拍照，检查墨水屏图像方向 | 图像方向正确（竖屏显示），与实际场景方向一致 |
| CAP-09 | 摄像头采集失败 | 遮挡摄像头后按按钮（模拟采集异常） | 串口输出 `[CAM] Capture failed!`，不崩溃，返回待机 |
| CAP-10 | 处理时间 | 拍照后观察串口时间输出 | `[IMG] Dither: xxx ms` < 2000ms，`[IMG] Total: xxx ms` 合理（不含屏幕刷新时间） |

### 3.3.3 存储管理测试

| **#** | **测试项** | **操作步骤** | **预期结果** |
| --- | --- | --- | --- |
| STO-01 | 照片保存格式 | 拍照后通过串口或读取 Flash 确认文件 | 文件名为 `/photos/00000.jpg`，为有效 JPEG 文件（可被图片查看器打开） |
| STO-02 | 照片文件大小 | 观察串口 `[FS] Saved` 输出中的字节数 | 每张约 5~15KB（QVGA JPEG 典型大小） |
| STO-03 | 当前照片覆盖 | 连续拍 2 张照片，检查 `/photos/current.jpg` | `current.jpg` 始终为最新一张照片 |
| STO-04 | 照片计数持久化 | 拍 3 张照片 → 断电 → 重新上电 → 观察启动画面照片数 | 显示 `Photos: 3`，计数未重置 |
| STO-05 | 序号连续递增 | 连续拍 5 张照片 | 文件名依次为 `00000.jpg` ~ `00004.jpg`，无跳号 |
| STO-06 | 上限自动清理 | 将 MAX_PHOTOS 临时改为 5，连拍 6 张 | 第 6 张保存后，`00000.jpg` 被自动删除，剩余 `00001.jpg` ~ `00005.jpg` |
| STO-07 | 存储容量显示 | 拍照后重启，观察启动画面存储信息 | `Storage: xx/xxxx KB` 数值合理，已用空间随拍照递增 |

### 3.3.4 电源与稳定性测试

| **#** | **测试项** | **操作步骤** | **预期结果** |
| --- | --- | --- | --- |
| PWR-01 | 上电启动速度 | 打开电源开关，计时到启动画面显示 | 从开关打开到墨水屏显示 Ready 画面 < 3 秒 |
| PWR-02 | 断电安全 | 拍照过程中（屏幕刷新中）直接关闭开关 | 设备断电，重新上电后系统正常，照片计数不丢失 |
| PWR-03 | 拍照中写入中断 | 在串口输出 `[FS] Saved` 之前关闭开关 | 重新上电后系统正常，已保存的照片完整，未完成的照片不影响系统 |
| PWR-04 | 连续拍照稳定性 | 连续拍 20 张照片（每次等待前一张完成后再拍） | 全部拍照成功，无内存泄漏或崩溃，串口输出正常 |
| PWR-05 | 长时间待机 | 上电后不按按钮，等待 10 分钟 | 设备保持待机状态，墨水屏维持启动画面，不自动休眠 |
| PWR-06 | PSRAM 内存管理 | 连续拍 10 张照片，观察串口是否有 PSRAM alloc failed | 无内存分配失败错误，每次拍照后正确释放 PSRAM |

### 3.3.5 边界与异常测试

| **#** | **测试项** | **操作步骤** | **预期结果** |
| --- | --- | --- | --- |
| EDG-01 | Flash 写满 | 连续拍照直到 Flash 接近满（观察 Storage 输出） | 系统不崩溃，最后几张可能保存失败但不影响后续操作 |
| EDG-02 | 极端光照 — 全白 | 拍摄纯白背景（白色纸张近距离） | 屏幕显示以白色为主的图像，抖动算法不产生全黑/全红异常 |
| EDG-03 | 极端光照 — 全黑 | 在完全黑暗环境拍摄 | 屏幕显示以黑色为主的图像，抖动算法不产生全白异常 |
| EDG-04 | 首次启动（空 Flash） | 擦除 Flash 后首次上电 | 启动画面显示 `Photos: 0`，`Storage: 0/xxxx KB`，正常进入待机 |
| EDG-05 | 快速连续按按钮 | 在前一张照片处理过程中再次按下按钮 | 等待当前拍照完成后再响应，不产生竞态或内存冲突 |